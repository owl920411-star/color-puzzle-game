# BLOCK ITEMS V2 — 검증 범위

기준 DEV: d7df0ee0e5ac16b1fab7a08e5161f1a4dd7b2c08.

실행: `node --test tests/block-items*.test.cjs`
결과: 48 tests, 48 pass, 0 fail.

이 중 한 테스트는 실게임과 동일한 `BlockItems.resolve` 함수를 무작위 보드 3,000개에 실행한다. 보드 크기, 중복 셀, 원본 보존, 미라/일반 줄 판정을 확인한다. 가상의 생존시간을 더하고 빼는 계산이 아니다. ‘3,000판 플레이’라는 의미도 아니다.

6종 효과, 추가 아이템 점수 없음, 일반 1~4줄 점수, NOVICE 7 기본 낙하, 미라 2회 완성, 착지 전 타이머 정지, 스카라베 한 번만 실패, 현재 블록 보호, 천장 보호, 봉인 변환 상한, 정화 보상 예약, 재시작·일시정지·아이템 OFF·DEV 기록 격리 등을 검사했다.

`block-items-app.test.cjs`는 실제 일반 엔진과 실제 앱 소스를 VM에서 실행한다. DOM/캔버스/스케줄러는 대체물이다. 따라서 실제 휴대폰 화면, 터치 반응, 렌더링 성능, 모래 모드의 물리 실행, 사용자의 7분 생존 목표는 검증되지 않았다.

엔진 로컬 사본은 저장소 blob 9556069dfd3b2cbcbbbf30f4cb542f67dea40aef와 SHA가 일치함을 확인했다. 엔진, 모래 엔진, 오른쪽 UI CSS는 이번 쓰기 대상에 포함하지 않는다.

Cloudflare 배포 완료는 GitHub 커밋 성공과 별개다. 배포 상태가 확인되기 전에는 새 버전이 링크에서 실행된다고 단정하지 않는다.

const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),E=require('../dist/engine');
const app=fs.readFileSync(require.resolve('../dist/app.js'),'utf8');let blocked=true;const node={classList:{add(){}},append(){}};
const c={Puzzle:{...E,moveBlocker:()=>blocked?{id:2,at:[2,0]}:null},stage:1,n:8,arrows:[{id:1,cells:[[0,0],[1,0]]}],hearts:3,moving:new Set(),blockedUntil:new Map(),epoch:1,developer:false,unlimited:false,mistakes:0,hints:0,$:()=>node,document:{querySelector:()=>node},clearGuides(){},status(){},tone(){},hud(){},point:p=>p,el:()=>node,setTimeout(){},showResult(){},drawFloor(){},fly:async()=>{},checkComplete(){}};
vm.createContext(c);vm.runInContext(app.slice(app.indexOf('async function tap('),app.indexOf('\nfunction fly(')),c);
(async()=>{assert.equal((await c.tap(1)).status,'blocked');assert.equal(c.hearts,2);assert.equal((await c.tap(1)).status,'blocked');assert.equal(c.hearts,2,'Repeated blocked tap does not charge twice');blocked=false;assert.equal((await c.tap(1)).status,'removed','Newly opened exit must ignore previous blocked cooldown');assert.equal(c.hearts,2);
const n=8,s=416/n,a={id:4,cells:[[0,0],[1,0]]};for(const offset of [-.02,0,.02])assert.equal(E.pick(32+s+offset,32+s/2,[a],n),4,'Connection across boundary accepts input');assert.equal(E.pick(32+2.5*s,32+s/2,[a],n),null,'Empty cell never selects neighbor');console.log('PASS: blocked-to-open immediate tap, duplicate penalty protection, seams and empty cells.');})();
